//! This module is for the exection of the debuggee program.
use std::error::Error;

// Get the modified binary file and run it.
pub fn continue_debugee(obj: &object::File) -> Result<(), Box<dyn Error>> {
    Ok(())
}
